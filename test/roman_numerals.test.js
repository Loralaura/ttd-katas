import { romanNumerals } from "../src/roman_numerals";

describe("romanNumerals", () => {
  test("test1", () => {
    expect(romanNumerals()).toBe(true);
    expect(romanNumerals()).toBe(true);
  });

  test("test3", () => {
    expect(romanNumerals()).toBe(true);
  });
});
